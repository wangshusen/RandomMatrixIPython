# Prerequisite

The IPython Notebook is required: http://ipython.org/notebook.html
If you¡¯re new to Python, you are highly recommended installing Anaconda. 
Then you should run the following command in the Terminal (Mac/Linux) or Command Prompt (Windows) to install Jupyter: ``conda install jupyter''.


# Get Start

Run ``ipython notebook'' or ``jupyter notebook''. This should result in the Notebok web application opening in your default web browser.